{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Aura Trade 桌面壳默认权限：只使用 core 权限（窗口/事件/路径）",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
